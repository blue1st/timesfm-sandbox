name: "Release"

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  publish-release:
    permissions:
      contents: write
      pull-requests: write
      repository-projects: write
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: macos-14
            arch: arm64
          - os: macos-14
            arch: x64
    steps:
      - uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: update package.json version from tag
        run: |
          VERSION=${{ github.ref_name }}
          # Remove 'v' prefix if present
          VERSION_NUM=${VERSION#v}
          npm version $VERSION_NUM --no-git-tag-version --allow-same-version

      - name: install dependencies
        run: npm install

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          pip install -r backend/requirements.txt
          pip install pyinstaller

      - name: Build Python backend
        run: |
          python -m PyInstaller --name server --onefile --target-architecture ${{ matrix.arch == 'x64' && 'x86_64' || 'arm64' }} --distpath build-python/backend backend/server.py

      - name: build and release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run dist -- --mac --${{ matrix.arch }} -p always

  update-tap:
    needs: publish-release
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: update package.json version from tag
        run: |
          VERSION=${{ github.ref_name }}
          VERSION_NUM=${VERSION#v}
          npm version $VERSION_NUM --no-git-tag-version --allow-same-version

      - name: Download artifacts from release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG_NAME=${{ github.ref_name }}
          gh release edit "$TAG_NAME" --draft=false || true
          gh release download "$TAG_NAME" --pattern "*.dmg" --dir .
          ls -R

      - name: Update Homebrew Tap
        env:
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          if [ -d "scripts" ] && [ -f "scripts/update-homebrew.sh" ]; then
            chmod +x scripts/update-homebrew.sh
            ./scripts/update-homebrew.sh
          else
            echo "Warning: scripts/update-homebrew.sh not found."
          fi
